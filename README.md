# Monthly Planner

A responsive monthly cost planner converted from the original ChatGPT Site into a complete Spring Boot application.

The full Spring Boot source, REST API, database configuration, Docker deployment, CI, and GitHub Pages publishing files are being added in the next commit.